# 运行时第三方依赖（其余均为 Python 标准库或 src 自有模块）
PyMySQL>=1.1.0  # MySQL 持久化（数据存储）
requests>=2.31.0  # src/beidan 抓取数据
# 生产曾执行“移除未使用包”，不能再依赖 requests 的隐式传递依赖。
urllib3>=2.2.0
certifi>=2024.2.2
charset-normalizer>=3.3.0
idna>=3.6
APScheduler>=3.10.4  # 赛果回填、快乐8定时任务
numpy>=2.1.0
pandas>=2.2.0  # 彩票 ML 特征整形
scipy>=1.14.0  # 诊断统计检验
scikit-learn>=1.5.2
lightgbm>=4.5.0
xgboost-cpu>=2.0.0  # 使用 CPU 版本避免 NVIDIA 依赖
catboost>=1.2.5

# ---- 架构重构：技术底座依赖（2026-08-25 起）----
fastapi>=0.115.0  # API 层框架
uvicorn>=0.32.0  # ASGI 服务器，取代 http.server
pydantic>=2.9.0  # 出入参契约，fastapi 依赖但显式登记
starlette>=0.40.0  # fastapi 依赖，显式登记
SQLAlchemy>=2.0.36  # Core 层，Repository 之下的 SQL 构造
redis>=5.2.0  # 缓存 L2 后端
