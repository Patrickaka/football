"""数字彩票领域基座。

与 `domain/sports` 并列而不合并：两者除「都要抓数据、都要缓存、都要回测」
之外没有共同的领域模型。赛事盘口的核心是比赛、赔率、结算；数字彩票的核心是
开奖号码、选号策略、候选集、命中回测。硬抽一个统一的 Predictor 接口，
最后只会退化成 `if business_type == ...` 的胶水。

跨领域的共享一律只发生在 `foundation/`。
"""
