"""福彩 3D 的领域实现。

与 kl8 的差别不只是号码范围，而是**结构**：kl8 一期开 20 个号、是个集合，
3D 一期开 3 个号、是个**有序三元组**，同一个数字可以重复出现。所以「频率」
「遗漏」这些词在两边指的不是一回事——3D 的绝大多数特征是**按位**统计的，
百位的 7 和个位的 7 是两个不同的量。

另一处必须记住的差别：**这里的历史序列是旧在前、新在后**
（`numbers[-1]` 是最近一期），而 `domain/numeric/statistics.py` 的
`draws` 是新在前。两者直接对接会让冷热、遗漏、趋势全部反过来，而且不报错。
"""
