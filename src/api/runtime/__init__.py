# -*- coding: utf-8 -*-
"""应用层运行时：缓存装配、后台任务、惰性加载的业务模块。

从 `src/webapp` 搬过来的——那个包原本是「旧 HTTP 入口 + 它用到的一切」，
入口删掉后剩下的这些其实是**应用层基础设施**，不属于任何一个业务域，
也不该留在一个以「web 层」命名的包里。

与 `src/foundation` 的区别：foundation 是与业务无关的技术底座
（缓存原语、任务调度器、抓取客户端）；这里是**这个应用**怎么把它们
装配起来——哪些结果要缓存、缓存多久、哪些任务要周期跑。
"""
